apiVersion: v1
kind: Service
metadata:
  name: { { include "warpflow-runtime.fullname" . } }
  labels: { { - include "warpflow-runtime.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "warpflow-runtime.selectorLabels" . | nindent 4 } }
